# ============================================================================
# .gitignore — Geo-SmartSpanner
# ============================================================================

# Python caches
__pycache__/
*.pyc
*.pyo

# Virtual environments
venv/
.venv/

# OSMnx cache (regenerate automatically on first run — do NOT commit, ~100MB+)
cache/

# LaTeX build junk (keep only .tex and .pdf)
paper/*.aux
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.fls
paper/*.fdb_latexmk

# OS / editor junk
.DS_Store
.vscode/
.idea/

# Large trained checkpoints (uncomment to exclude from git; use Git LFS instead)
# results/models/*.pt
# best_gnn_model.pt
# best_base_model.pt
