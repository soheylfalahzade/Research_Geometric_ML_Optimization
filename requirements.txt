# ============================================================================
# requirements.txt — Geo-SmartSpanner (Research_Geometric_ML_Optimization)
# فقط پکیج‌هایی که واقعاً در کد پروژه import می‌شوند؛ نسخهٔ قبلی این فایل
# خروجی کامل conda/pip freeze بود (بیش از ۱۵۰ پکیج نامرتبط مثل streamlit،
# telegram-bot، selenium، sumo/traci، ultralytics). آن‌ها حذف شدند.
# ============================================================================

# --- هستهٔ عددی / گراف ---
numpy>=1.26,<3.0
scipy>=1.13
networkx>=3.3
pandas>=2.2

# --- یادگیری عمیق ---
torch>=2.3
torch-geometric>=2.5
scikit-learn>=1.5

# --- داده‌های مکانی / OSM ---
osmnx>=1.9
pyrosm>=0.6  # used by benchmarks/scale_test_tokyo.py and leave_one_scale_out.py (local PBF loading)

# --- رسم و گزارش ---
matplotlib>=3.9
seaborn>=0.13
folium>=0.16
